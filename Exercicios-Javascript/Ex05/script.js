var numeroUm = Number(prompt("Digite um numero para fazer um cálculo:"))
var numeroDois = Number(prompt("Digite outro numero para efetuar uma soma:"))
var resultado = numeroUm + numeroDois

alert(`
    O resultado dessa soma é:
     ${numeroUm} + ${numeroDois} = ${resultado}
    `)